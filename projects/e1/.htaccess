DirectoryIndex digital-portfolio.php
RewriteEngine On

# Ajoute un slash final si absent (sauf fichiers/dossiers réels)
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d
RewriteRule ^([^/]+)$ /$1/ [R=301,L]

# Redirige /nompage/ vers /nompage.html si le fichier existe
RewriteCond %{REQUEST_FILENAME}.php -f
RewriteRule ^([^/]+)/$ $1.php [L,QSA]
